export default async function generateQuizQuestion({
  subject,
  difficulty,
  numberOfQuestions,
}: {
  subject: string;
  difficulty: string;
  numberOfQuestions: number;
}) {
  // Your logic to generate quiz questions goes here
  return []; // Return the generated questions
}